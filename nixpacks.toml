[phases.setup]
nixPkgs = ["python311", "ffmpeg", "gcc", "g++"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
cmd = "uvicorn src.app:app --host 0.0.0.0 --port $PORT"

